---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "incentives-admin.fullname" . }}
  labels:
    {{- include "incentives-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incentives-admin.selectorLabels" . | nindent 4 }}
{{- if .Values.pm2_metrics.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "incentives-admin.fullname" . }}-pm2-metrics
  labels:
    {{- include "incentives-admin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.pm2_metrics.port }}
    name: pm2-metrics
    targetPort: {{ .Values.pm2_metrics.port }}
  selector:
    {{- include "incentives-admin.selectorLabels" . | nindent 4 }}
{{- end }}
{{- if .Values.metrics.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "incentives-admin.fullname" . }}-metrics
  labels:
    {{- include "incentives-admin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.metrics.port }}
    name: metrics
    targetPort: {{ .Values.metrics.port }}
  selector:
    {{- include "incentives-admin.selectorLabels" . | nindent 4 }}
{{- end }}
