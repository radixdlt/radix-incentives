"use client";

import type { FC } from "react";

/**
 * Displays a message indicating there are no active consultations.
 */
export const NoActiveConsultationMessage: FC = () => {
  return <p>There are no active community consultations at this time.</p>;
};
